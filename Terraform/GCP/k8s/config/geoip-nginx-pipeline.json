PUT _ingest/pipeline/geoip-nginx
{
  "description" : "Add GeoIP info to Nginx logs",
  "processors" : [
    {
      "geoip" : {
        "field" : "network.source_ip",
        "target_field" : "network.geo"
      }
    }
  ]
}
